% AEGIS Framework Architecture Diagram — Horizontal Layout
% Landscape, IEEE two-column width (~7in), matching reference image style
\documentclass[11pt,border=3pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath,amssymb}
\usepackage{tikz}
\usetikzlibrary{
  positioning,
  arrows.meta,
  calc,
  fit,
  backgrounds,
  decorations.pathreplacing,
  shapes.geometric,
  shadows.blur
}
\usepackage{xcolor}

% ── Color palette ──────────────────────────────────────────────────
\definecolor{inputbg}{HTML}{FAFAFA}
\definecolor{layer1}{HTML}{D6EAF8}
\definecolor{layer1dark}{HTML}{5DADE2}
\definecolor{layer2}{HTML}{D5F5E3}
\definecolor{layer2dark}{HTML}{58D68D}
\definecolor{layer3}{HTML}{FDEBD0}
\definecolor{layer3dark}{HTML}{F0B27A}
\definecolor{layer4}{HTML}{E8DAEF}
\definecolor{layer4dark}{HTML}{AF7AC5}
\definecolor{appbg}{HTML}{F2F3F4}
\definecolor{appborder}{HTML}{BDC3C7}
\definecolor{complexgray}{HTML}{7F8C8D}
\definecolor{routerbg}{HTML}{F8F9F9}
\definecolor{glowcolor}{HTML}{F39C12}

\pagestyle{empty}
\newcommand{\cpx}[1]{{\sffamily\fontsize{5}{6}\selectfont\color{complexgray}#1}}

\begin{document}
\begin{tikzpicture}[
  >=Stealth,
  font=\sffamily\small,
  node distance=0.2cm,
  block/.style={
    rectangle, rounded corners=2.5pt,
    draw=#1!70!black, line width=0.45pt,
    fill=#1!25, minimum width=2.6cm, minimum height=1.0cm,
    inner sep=2.5pt, align=center,
    blur shadow={shadow blur steps=3, shadow xshift=0.3pt,
                 shadow yshift=-0.3pt, shadow blur radius=0.8pt}
  },
  block/.default=layer1dark,
  iobox/.style={
    rectangle, rounded corners=2.5pt,
    draw=black!25, fill=white,
    minimum width=1.4cm, minimum height=0.65cm,
    inner sep=2pt, align=center,
    blur shadow={shadow blur steps=2, shadow xshift=0.2pt,
                 shadow yshift=-0.2pt, shadow blur radius=0.5pt}
  },
  appblock/.style={
    rectangle, rounded corners=2pt,
    draw=appborder, fill=white,
    minimum width=2.85cm, minimum height=0.88cm,
    inner sep=2pt, align=center,
    font=\sffamily\tiny
  },
  flowarrow/.style={->, thick, color=black!60},
  layerarrow/.style={->, line width=1pt, color=black!55},
  interarrow/.style={->, line width=0.9pt, color=black!50},
  layerlabel/.style={
    rectangle, rounded corners=2pt,
    fill=#1, draw=#1!60!black, line width=0.4pt,
    minimum width=1.55cm, minimum height=1.0cm,
    inner sep=2pt, align=center,
    font=\sffamily\fontsize{5.5}{7}\selectfont\bfseries, text=black!75
  },
  pathbox/.style={
    rectangle, rounded corners=2.5pt,
    draw=#1!50!black, line width=0.45pt, dashed,
    fill=#1!6, inner sep=3pt
  },
  router/.style={
    diamond, draw=black!45, fill=routerbg,
    inner sep=0.8pt, font=\sffamily\fontsize{5}{6}\selectfont, aspect=2.2,
    minimum width=1.0cm
  },
  badge/.style={
    rectangle, rounded corners=1.5pt,
    fill=layer1!70, draw=layer1dark!60, line width=0.3pt,
    font=\sffamily\fontsize{4.5}{5.5}\selectfont\itshape, inner sep=1.2pt
  }
]

% ════════════════════════════════════════════════════════════════════
% MASTER LAYOUT — all blocks stay within [blkLeft .. blkRight]
% Label column is [0 .. 1.6], block area is [2.2 .. ~12.5]
% ════════════════════════════════════════════════════════════════════

\def\labelCX{0.8}        % center of label column
\def\blkLeft{2.2}        % left edge of block area

% Y anchors (more vertical space between layers to avoid overlaps)
\def\yL{0}
\def\yDense{-1.7}
\def\yMfree{-2.95}
\def\yThree{-4.75}
\def\yFour{-6.55}
\def\yOut{-7.65}
\def\yApp{-8.75}

% ── INPUT STRIP ───────────────────────────────────────────────────
\node[iobox] at (-1.3, 0.3) (gnn) {%
  {\fontsize{5.5}{7}\selectfont\bfseries Trained GNN}\\[-1pt]
  {\fontsize{5}{6}\selectfont $f_\theta$}};
\node[iobox, below=0.12cm of gnn] (graph) {%
  {\fontsize{5.5}{7}\selectfont\bfseries Target Graph}\\[-1pt]
  {\fontsize{5}{6}\selectfont $G=(X,A)$}};
\node[above=0.06cm of gnn,
      font=\sffamily\fontsize{5.5}{7}\selectfont\bfseries,
      text=black!55] {INPUTS};

\coordinate (inmerge) at ($(gnn.east)!0.5!(graph.east)+(0.2,0)$);
\draw[flowarrow] (gnn.east) -| (inmerge);
\draw[flowarrow] (graph.east) -| (inmerge);

% ══════════════════════════════════════════════════════════════════
% LAYER 1 — GRAPH PREPROCESSING
% ══════════════════════════════════════════════════════════════════

\node[layerlabel=layer1] at (\labelCX, \yL) (L1lbl) {%
  LAYER 1\\[1pt] Graph\\Preproc.};

\node[block=layer1dark] at (\blkLeft+1.3, \yL) (bfs) {%
  {\fontsize{5.5}{7}\selectfont\bfseries BFS Subgraph}\\[-1pt]
  {\fontsize{5}{6}\selectfont $A_{\mathrm{sub}}=A[\mathrm{idx}],\;N_{\mathrm{sub}}\!\le\!200$}\\[0pt]
  \cpx{$\mathcal{O}(N_{\mathrm{sub}})$}};

\node[block=layer1dark, right=0.3cm of bfs] (fwd) {%
  {\fontsize{5.5}{7}\selectfont\bfseries Forward Pass}\\[-1pt]
  {\fontsize{5}{6}\selectfont $Z = f_\theta(X, A)$}\\[0pt]
  \cpx{$\mathcal{O}(N\!\cdot\!d)$}};

\node[block=layer1dark, right=0.3cm of fwd] (fp) {%
  {\fontsize{5.5}{7}\selectfont\bfseries Fixed-Point Convergence}\\[-1pt]
  {\fontsize{5}{6}\selectfont $F(Z^*\!,A) = Z^*$\;{\color{complexgray}(IGNN)}}\\[0pt]
  \cpx{$\mathcal{O}(T\!\cdot\!Nd)$}};

\draw[flowarrow] (bfs.east) -- (fwd.west);
\draw[flowarrow] (fwd.east) -- (fp.west);
\draw[layerarrow] (inmerge) -- (bfs.west);

% ── L1 -> L2 connection (L-shaped: down from fp, left to router) ──
\coordinate (l1drop) at ($(fp.south)+(0,-0.35)$);
\draw[interarrow] (fp.south) -- (l1drop)
  node[midway, right, font=\sffamily\fontsize{4.5}{5.5}\selectfont,
       text=black!45, xshift=1pt]
  {$Z^*\!,\;A_{\mathrm{sub}},\;\mathrm{ctx}$};

% ══════════════════════════════════════════════════════════════════
% LAYER 2 — SENSITIVITY COMPUTATION
% ══════════════════════════════════════════════════════════════════

\node[layerlabel=layer2, minimum height=2.3cm]
  at (\labelCX, {(\yDense+\yMfree)/2}) (L2lbl) {%
  LAYER 2\\[1pt] Sensitivity\\Computation};

\node[router] at (\blkLeft+0.5, {(\yDense+\yMfree)/2}) (router) {$N\!\le\!200$?};

% L-shaped arrow: down then left to router
\draw[interarrow] (l1drop) |- (router.east);

% ── Dense Path ────────────────────────────────────────────────────
\node[block=layer2dark, minimum width=2.4cm] at (\blkLeft+3.4, \yDense) (Jz) {%
  {\fontsize{5.5}{7}\selectfont\bfseries $J_z$ via Autograd}\\[-1pt]
  {\fontsize{5}{6}\selectfont $\partial F/\partial\!\operatorname{vec}(Z)$}\\[0pt]
  \cpx{$\mathcal{O}((Nd)^2)$}};

\node[block=layer2dark, minimum width=2.4cm, right=0.22cm of Jz] (Ja) {%
  {\fontsize{5.5}{7}\selectfont\bfseries $J_A$ via Autograd}\\[-1pt]
  {\fontsize{5}{6}\selectfont $\partial F/\partial\!\operatorname{vec}(A)$}\\[0pt]
  \cpx{$\mathcal{O}(Nd\!\cdot\!N^2)$}};

\node[block=layer2dark, minimum width=2.55cm, right=0.22cm of Ja] (lsolve) {%
  {\fontsize{5.5}{7}\selectfont\bfseries Linear Solve}\\[-1pt]
  {\fontsize{5}{6}\selectfont $S = (I - J_z)^{-1} J_A$}\\[0pt]
  \cpx{$\mathcal{O}((Nd)^3)$ \textbf{bottleneck}}};

\draw[flowarrow] (Jz.east) -- (Ja.west);
\draw[flowarrow] (Ja.east) -- (lsolve.west);

% ── Matrix-Free Path ─────────────────────────────────────────────
\node[block=layer2dark, minimum width=2.4cm] at (\blkLeft+3.4, \yMfree) (rho) {%
  {\fontsize{5.5}{7}\selectfont\bfseries $\rho$ Estimation}\\[-1pt]
  {\fontsize{5}{6}\selectfont Power iteration}\\[0pt]
  \cpx{$\mathcal{O}(30\!\cdot\!Nd)$}};

\node[block=layer2dark, minimum width=2.4cm, right=0.22cm of rho] (jvp) {%
  {\fontsize{5.5}{7}\selectfont\bfseries JVPs}\\[-1pt]
  {\fontsize{5}{6}\selectfont \texttt{torch.func.jvp}}\\[0pt]
  \cpx{$\mathcal{O}(Nd)$/prod}};

\node[block=layer2dark, minimum width=2.55cm, right=0.22cm of jvp] (neum) {%
  {\fontsize{5.5}{7}\selectfont\bfseries Neumann Series}\\[-1pt]
  {\fontsize{5}{6}\selectfont $\sum_{k=0}^{K} J_z^k\, b$}\\[0pt]
  \cpx{$\mathcal{O}(K\!\cdot\!Nd)$}};

\draw[flowarrow] (rho.east) -- (jvp.west);
\draw[flowarrow] (jvp.east) -- (neum.west);

% Router to paths
\draw[flowarrow, densely dashed]
  (router.north) |- (Jz.west)
  node[pos=0.25, above, font=\sffamily\fontsize{4.5}{5.5}\selectfont\bfseries,
       text=black!50] {Yes};
\draw[flowarrow, densely dashed]
  (router.south) |- (rho.west)
  node[pos=0.25, below, font=\sffamily\fontsize{4.5}{5.5}\selectfont\bfseries,
       text=black!50] {No};

% Dashed containers
\begin{scope}[on background layer]
  \node[pathbox=layer2dark, fit=(Jz)(Ja)(lsolve), inner sep=3pt,
        label={[font=\sffamily\fontsize{4.5}{5.5}\selectfont\itshape,
               text=black!40, anchor=north west, xshift=2pt, yshift=-1pt]
               north west:Dense $(N\!\le\!200)$}] (densefit) {};
  \node[pathbox=layer2dark, fit=(rho)(jvp)(neum), inner sep=3pt,
        label={[font=\sffamily\fontsize{4.5}{5.5}\selectfont\itshape,
               text=black!40, anchor=north west, xshift=2pt, yshift=-1pt]
               north west:Matrix-Free $(N\!>\!200)$}] (mffit) {};
\end{scope}

% ── L2 merge (center below both paths) ───────────────────────────
\coordinate (l2merge) at ($(neum.south)+(0,-0.45)$);
\draw[flowarrow] (lsolve.south) -- ++(0,-0.2) -| (l2merge);
\draw[flowarrow] (neum.south)   -- (l2merge);

% ══════════════════════════════════════════════════════════════════
% LAYER 3 — CONSTRAINED PROJECTION (highlighted, key contribution)
% ══════════════════════════════════════════════════════════════════

\node[layerlabel=layer3] at (\labelCX, \yThree) (L3lbl) {%
  LAYER 3\\[1pt] Constrained\\Projection};

% Block anchored at blkLeft, NOT overlapping the label
\node[rectangle, rounded corners=3pt,
      draw=glowcolor, line width=1.6pt,
      fill=layer3!40, minimum height=1.15cm,
      align=center, anchor=west,
      blur shadow={shadow blur steps=5, shadow xshift=0.4pt,
                   shadow yshift=-0.4pt, shadow blur radius=1.5pt}]
  at (\blkLeft+0.05, \yThree) (proj) {%
  \quad
  {\small\bfseries $S \;\longrightarrow\; \mathcal{P}_c \;\longrightarrow\; S_c$}%
  \qquad
  {\fontsize{5.5}{7}\selectfont
    $[S_c]_{:,k} = S_{:,iN\!+\!j} + S_{:,jN\!+\!i}$
    \; for each edge $(i,j)\!\in\!\mathcal{E}$}%
  \quad};

% Second line inside proj area: complexity info
\node[below=0.02cm of proj.south, anchor=north,
      font=\sffamily\fontsize{4.5}{5.5}\selectfont, text=complexgray]
  (projcpx) {$N^2\!\to\!|\mathcal{E}|$ dims
   \;$\cdot$\; symmetric + edge-only
   \;$\cdot$\; $\mathcal{O}(|\mathcal{E}|)$};

% "central contribution" label above
\node[above=0.02cm of proj.north,
      font=\sffamily\fontsize{4.5}{5.5}\selectfont\itshape,
      text=glowcolor!80!black] (l3title) {central technical contribution};

% Arrow L2 -> L3 with annotation (left side so it stays in-bounds)
\draw[interarrow] (l2merge) -- (proj.north)
  node[midway, left, font=\sffamily\fontsize{4.5}{5.5}\selectfont,
       text=black!45, xshift=-1pt]
  {$S\!\in\!\mathbb{R}^{Nd\times N^2}$};

% ── L3 -> L4 drop with S_c annotation on the arrow ───────────────
\coordinate (l3drop) at ($(projcpx.south)+(0,-0.35)$);
\draw[interarrow] (projcpx.south) -- (l3drop)
  node[midway, left, font=\sffamily\fontsize{4.5}{5.5}\selectfont,
       text=black!45, xshift=-1pt]
  {$S_c\!\in\!\mathbb{R}^{Nd\times|\mathcal{E}|}$};

% ══════════════════════════════════════════════════════════════════
% LAYER 4 — VULNERABILITY ANALYSIS (3 parallel outputs)
% ══════════════════════════════════════════════════════════════════

\node[layerlabel=layer4] at (\labelCX, \yFour) (L4lbl) {%
  LAYER 4\\[1pt] Vulnerability\\Analysis};

\node[block=layer4dark, minimum width=2.85cm] at (\blkLeft+1.5, \yFour) (svd) {%
  {\fontsize{5.5}{7}\selectfont\bfseries Randomized SVD}\\[-1pt]
  {\fontsize{5}{6}\selectfont $(U,\sigma,V) = \mathrm{rSVD}(S_c,\,k\!=\!6)$}\\[0pt]
  \cpx{$\mathcal{O}(k\!\cdot\!n_{\mathrm{iter}}\!\cdot\!Nd)$}};

\node[block=layer4dark, minimum width=2.85cm, right=0.25cm of svd] (cnorm) {%
  {\fontsize{5.5}{7}\selectfont\bfseries Column Norms}\\[-1pt]
  {\fontsize{5}{6}\selectfont $v_k = \|[S_c]_{:,k}\|_2$}\\[0pt]
  \cpx{$\mathcal{O}(|\mathcal{E}|\!\cdot\!Nd)$}};

\node[block=layer4dark, minimum width=2.85cm, right=0.25cm of cnorm] (radius) {%
  {\fontsize{5.5}{7}\selectfont\bfseries Per-Node Radius}\\[-1pt]
  {\fontsize{5}{6}\selectfont $r_v = m_v / (\|\nabla f\|\!\cdot\!\|S_v\|)$}\\[0pt]
  \cpx{$\mathcal{O}(N\!\cdot\!d)$}};

% Fan-out from L3 drop point to L4 blocks
\draw[flowarrow] (l3drop) -| (svd.north);
\draw[flowarrow] (l3drop) -| (cnorm.north);
\draw[flowarrow] (l3drop) -| (radius.north);

% ── Output boxes ──────────────────────────────────────────────────
\node[iobox, below=0.2cm of svd, minimum width=2.6cm] (out1) {%
  {\fontsize{5.5}{7}\selectfont\bfseries Optimal Attack $\delta A^*$}\\[-1pt]
  {\fontsize{4.5}{5.5}\selectfont SVD-optimal perturbation}};

\node[iobox, below=0.2cm of cnorm, minimum width=2.6cm] (out2) {%
  {\fontsize{5.5}{7}\selectfont\bfseries Vuln.\ Spectrum $v_{ij}$}\\[-1pt]
  {\fontsize{4.5}{5.5}\selectfont Per-edge rankings}};

\node[iobox, below=0.2cm of radius, minimum width=2.6cm] (out3) {%
  {\fontsize{5.5}{7}\selectfont\bfseries Sens.\ Radii $r_v$}\\[-1pt]
  {\fontsize{4.5}{5.5}\selectfont Per-node tolerance}};

\draw[flowarrow] (svd.south) -- (out1.north);
\draw[flowarrow] (cnorm.south) -- (out2.north);
\draw[flowarrow] (radius.south) -- (out3.north);

\node[badge, right=0.06cm of out3.east]
  {+ $\varepsilon_{\mathrm{crit}}$ {\itshape IGNN only}};

% ══════════════════════════════════════════════════════════════════
% APPLICATION EXTENSIONS (bottom strip)
% ══════════════════════════════════════════════════════════════════

\node[layerlabel=appbg, draw=appborder, minimum height=0.88cm,
      text=black!60] at (\labelCX, \yApp) (Applbl) {%
  APPLICATION\\EXTENSIONS};

\node[appblock] at (\blkLeft+1.5, \yApp) (app1) {%
  {\fontsize{5.5}{7}\selectfont\bfseries Defense Design}\\[1pt]
  {\fontsize{4.5}{5.5}\selectfont Mask top-$k$ vulnerable edges}\\
  {\fontsize{4.5}{5.5}\selectfont from perturb.\ space (Sec.\ V-F)}};

\node[appblock, right=0.18cm of app1] (app2) {%
  {\fontsize{5.5}{7}\selectfont\bfseries Power Flow N-1}\\[1pt]
  {\fontsize{4.5}{5.5}\selectfont $v_{ij}\!\approx$ line-trip severity}\\
  {\fontsize{4.5}{5.5}\selectfont ($\tau = 0.37$--$0.72$)}};

\node[appblock, right=0.18cm of app2] (app3) {%
  {\fontsize{5.5}{7}\selectfont\bfseries Explicit GNN Extension}\\[1pt]
  {\fontsize{4.5}{5.5}\selectfont $S_K$ via unrolled Jacobian}\\
  {\fontsize{4.5}{5.5}\selectfont (Observation 1)}};

\node[appblock, right=0.18cm of app3] (app4) {%
  {\fontsize{5.5}{7}\selectfont\bfseries Cross-Domain Transfer}\\[1pt]
  {\fontsize{4.5}{5.5}\selectfont 7 archs $\times$ 9 datasets}\\
  {\fontsize{4.5}{5.5}\selectfont ($\tau > 0$ in 29/33)}};

\draw[densely dashed, ->, black!30, line width=0.5pt]
  (out1.south) -- ++(0,-0.1) -| (app1.north);
\draw[densely dashed, ->, black!30, line width=0.5pt]
  (out2.south) -- ++(0,-0.1) -| (app2.north);
\draw[densely dashed, ->, black!30, line width=0.5pt]
  (out2.south) -- ++(0,-0.1) -| (app3.north);
\draw[densely dashed, ->, black!30, line width=0.5pt]
  (out3.south) -- ++(0,-0.1) -| (app4.north);

% ══════════════════════════════════════════════════════════════════
% BACKGROUND LAYER BANDS
% ══════════════════════════════════════════════════════════════════
\begin{scope}[on background layer]
  \node[rectangle, rounded corners=3pt, fill=layer1!22,
        fit=(L1lbl)(bfs)(fwd)(fp), inner sep=6pt] {};
  \node[rectangle, rounded corners=3pt, fill=layer2!18,
        fit=(L2lbl)(router)(densefit)(mffit), inner sep=6pt] {};
  \node[rectangle, rounded corners=3pt, fill=layer3!18,
        fit=(L3lbl)(proj)(projcpx), inner sep=6pt] {};
  \node[rectangle, rounded corners=3pt, fill=layer4!18,
        fit=(L4lbl)(svd)(cnorm)(radius)(out1)(out2)(out3), inner sep=6pt] {};
  \node[rectangle, rounded corners=3pt, fill=appbg!45,
        fit=(Applbl)(app1)(app2)(app3)(app4), inner sep=5pt] {};
\end{scope}

\end{tikzpicture}
\end{document}
